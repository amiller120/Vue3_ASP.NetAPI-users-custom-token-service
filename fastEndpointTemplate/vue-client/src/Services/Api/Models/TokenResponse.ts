export interface TokenResponse {
    userId: string;
    refreshToken: string;
    accessToken: string;
    accessExiry: Date;
    refreshExpiry: Date;
    accessTokenExpiray: string;
    refreshTokenValidityMinutes: number;
}
